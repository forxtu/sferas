import styled from 'styled-components';

// Styles
import indents from 'styles/partials/indents';

const AddGoalWrapper = styled.div`
  input {
    margin-bottom: ${indents.medium};
  }

  button {
    margin-right: ${indents.small};
  }
`;

export { AddGoalWrapper };
